'use client';

import * as React from 'react';

export function AssetsTab() {
  return (
    <div className="cv-container p-10 text-center">
      <h3 className="text-lg font-semibold" style={{ color: 'var(--text-primary)' }}>Compute Assets</h3>
      <p className="mt-2 text-sm" style={{ color: 'var(--text-tertiary)' }}>
        Inventory of EC2, Lambda, and Container resources coming soon.
      </p>
    </div>
  );
}
